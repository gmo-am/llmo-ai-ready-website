# VCS
.git
.gitignore

# Node
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-lock.yaml

# Next.js build output (built inside container)
.next

# Environment files (never send to build context)
.env*

# Others
Dockerfile
cloudbuild.yaml
